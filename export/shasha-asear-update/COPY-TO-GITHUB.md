# نشر التحديث على shasha-asear

إذا لم يُرفع التحديث تلقائياً، من جهازك:

```bash
cd shasha-asear
git pull
git merge cursor/mobile-tv-mt5-relay-2bd9
# أو انسخ الملفات من هذا المجلد إلى مستودعك ثم:
git add -A && git commit -m "مزامنة MT5 للموبايل والتلفزيون" && git push
```
